# OpenAI Configuration
OPENAI_API_KEY=sk-your-openai-api-key-here
MODEL_DEFAULT=gpt-4o-mini

# Email Configuration (for lead notifications)
LEAD_MAIL_TO=info@kayacuneyt.com
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Optional: Webhook for lead notifications (Google Sheets, Zapier, etc.)
LEAD_WEBHOOK_URL=https://hooks.zapier.com/hooks/catch/your-webhook-url

# Server Configuration
PORT=3000
NODE_ENV=development

# Optional: Frontend URL for CORS (production)
FRONTEND_URL=https://yourdomain.com

# Security
# JWT_SECRET=your-jwt-secret-here (if implementing auth)

# Database (if using in future)
# DATABASE_URL=postgresql://user:password@localhost:5432/portfolio

# Rate Limiting (optional customization)
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100

# Cache Duration (optional customization)
# CACHE_DURATION_MS=300000
